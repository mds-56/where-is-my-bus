rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Testing only: public read/write access while the website is being built.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
